{
  "name": "roadmaps-universitarios",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "typecheck": "tsc --noEmit",
    "test": "npm run typecheck && npm run test:integration",
    "test:integration": "sh -c 'docker compose -f docker-compose.test.yml up -d --wait && trap \"docker compose -f docker-compose.test.yml down -v\" EXIT; DATABASE_URL=postgresql://roadmap_test_user:roadmap_test_password@localhost:5433/roadmap_test_db prisma migrate deploy && DATABASE_URL=postgresql://roadmap_test_user:roadmap_test_password@localhost:5433/roadmap_test_db prisma db seed && DATABASE_URL=postgresql://roadmap_test_user:roadmap_test_password@localhost:5433/roadmap_test_db tsx --test tests/roadmap-api.integration.ts'",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate deploy",
    "prisma:seed": "prisma db seed",
    "prisma:prepare": "prisma generate && prisma migrate deploy && prisma db seed"
  },
  "prisma": {
    "seed": "tsx prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "lucide-react": "^0.378.0",
    "next": "^14.2.3",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "reactflow": "^11.11.4"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "autoprefixer": "^10.4.19",
    "postcss": "^8.4.38",
    "prisma": "^5.22.0",
    "tailwindcss": "^3.4.3",
    "tsx": "^4.19.2",
    "typescript": "^5.4.5"
  }
}
