# AGENTS.md

## Agent skills

### Issue tracker

GitHub issues on `Bigelazo/u-roadmaps`, accessed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout — one `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.

### Visual design

Before implementing or changing user-facing UI, consult the development-only `/design-preview` laboratory and follow `docs/agents/design-preview.md`.
